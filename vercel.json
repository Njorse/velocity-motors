{
  "buildCommand": "cd velocity-react && npm install && npm run build",
  "outputDirectory": "velocity-react/dist",
  "installCommand": "cd velocity-react && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
